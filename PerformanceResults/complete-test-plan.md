# 完整性能测试执行计划

## 测试状态

- ✅ 场景 1：基线测量（已完成）
- 🔄 场景 2：笔记操作（进行中，只测了 6 次）
- ⏳ 场景 3：无限滚动
- ⏳ 场景 4：搜索性能
- ⏳ 场景 5：批量操作
- ⏳ 场景 6：长时间运行

---

## 测试目的

为什么要完成所有测试？

| 场景 | 可能发现的问题 | 不测试会遗漏 |
|------|---------------|-------------|
| 场景 2：笔记操作 | 状态更新频繁 | ✅ 已发现 |
| 场景 3：无限滚动 | `rowPositions` 无限增长 | 遗漏内存泄漏 |
| 场景 3：无限滚动 | `daySchedules` 无限增长 | 遗漏内存泄漏 |
| 场景 4：搜索性能 | `calculateSearchOccurrences` 性能 | 遗漏搜索卡顿 |
| 场景 4：搜索性能 | debounce 失效 | 遗漏性能问题 |
| 场景 5：批量操作 | 大量笔记同时更新 | 遗漏批量操作性能问题 |
| 场景 6：长时间运行 | 内存累积泄漏 | 遗漏长期使用问题 |

---

## 现在继续测试

### 场景 2 完整版：笔记操作（20 次）

**补充执行**：再操作 14 次（总共 20 次）

1. **在终端继续观察日志**：
   ```bash
   tail -f /tmp/agendada-perf.log
   ```

2. **在 Agendada 中执行**：
   - 新建笔记 → 输入标题 → 输入正文 → ESC
   - 重复 20 次（之前已做 6 次）

3. **记录关键指标**：
   - `publishChange()` 总调用次数：____
   - `filteredNotes()` 重新计算次数：____
   - 是否出现新的警告日志

---

### 场景 3：无限滚动（2 分钟）

**目标**：检测 `rowPositions` 和 `daySchedules` 无限增长

#### 步骤 1：准备
- 确保有至少 30 个笔记（如果不够，先创建）
- 在 Activity Monitor 中观察 Agendada 内存

#### 步骤 2：执行
1. 在笔记流中**快速上下滚动** 2 分钟
2. 在终端观察日志：
   ```bash
   tail -f /tmp/agendada-perf.log | grep -E "(rowPositions|daySchedules|mergeSchedules)"
   ```

#### 步骤 3：记录
```
场景 3：无限滚动（2 分钟）

内存变化：
- 操作前：___ MB
- 操作中峰值：___ MB
- 操作后：___ MB
- 增长：___ MB

日志观察：
- [ ] 看到大量 mergeSchedules 调用
- [ ] 看到 rowPositions 相关警告
- [ ] CPU 明显升高
```

**关键问题**：内存增长 > 50 MB？

---

### 场景 4：搜索性能（1 分钟）

**目标**：检测搜索计算和 debounce 效果

#### 步骤 1：准备
- 确保有 30+ 个笔记
- 打开搜索框

#### 步骤 2：执行
1. 在搜索框中快速输入：`agendada performance test search query`
2. 每个字符间隔 < 200ms
3. 在终端观察：
   ```bash
   tail -f /tmp/agendada-perf.log | grep -E "(search|filteredNotes)"
   ```

#### 步骤 3：记录
```
场景 4：搜索性能

输入 20 个字符，观察：
- calculateSearchOccurrences 调用次数：____（预期 < 20，因为有 debounce）
- filteredNotes() 重新计算次数：____
- 是否有卡顿：[是/否]
```

**关键问题**：
- 如果 `calculateSearchOccurrences` 调用 ≈ 20 → debounce 有效 ✅
- 如果调用 > 40 → debounce 失效 ❌

---

### 场景 5：批量操作（1 分钟）

**目标**：检测批量更新性能

#### 步骤 1：准备
- 确保有 20+ 个笔记

#### 步骤 2：执行
1. 点击第一个笔记的复选框（进入批量模式）
2. 点击 "全选"
3. 点击 "移动到项目" 或 "删除"
4. 重复 10 次

#### 步骤 3：记录
```
场景 5：批量操作

全选操作：
- CPU 峰值：___ %
- 是否卡顿：[是/否]

批量删除：
- 耗时：___ 秒
- publishChange 调用次数：____
```

**关键问题**：批量操作时是否卡顿？

---

### 场景 6：长时间运行（10 分钟）

**目标**：检测内存泄漏和性能退化

#### 步骤 1：准备
- 记录初始内存：___ MB

#### 步骤 2：执行
混合操作 10 分钟：
- 创建笔记（2 分钟）
- 滚动（2 分钟）
- 搜索（1 分钟）
- 切换笔记（2 分钟）
- 批量操作（1 分钟）
- 空闲（2 分钟）

#### 步骤 3：每 2 分钟记录
```
场景 6：长时间运行（10 分钟）

时间点 | 内存 | CPU | 观察
-------|------|-----|------
0 分钟  | ___ MB | ___ % | 基线
2 分钟  | ___ MB | ___ % | 
4 分钟  | ___ MB | ___ % | 
6 分钟  | ___ MB | ___ % | 
8 分钟  | ___ MB | ___ % | 
10 分钟 | ___ MB | ___ % | 
```

**关键问题**：内存是否持续增长？（> 50 MB 增长 = 泄漏）

---

## 📊 每个场景完成后

在每个场景测试完成后，告诉我：

### 场景 2（笔记操作）
- publishChange 总调用次数
- 是否发现新问题

### 场景 3（滚动）
- 内存增长量
- CPU 峰值

### 场景 4（搜索）
- 搜索调用次数
- 是否卡顿

### 场景 5（批量）
- 是否卡顿
- 操作耗时

### 场景 6（长运行）
- 内存趋势
- 性能是否退化

---

## 🎯 测试完成后的输出

完成所有 6 个场景后，我会提供：

1. **完整的问题清单**
   - Critical 问题列表
   - Warning 问题列表
   - Info 级别问题列表

2. **每个问题的证据**
   - 日志数据
   - 内存数据
   - CPU 数据

3. **修复优先级**
   - P0: 必须立即修复
   - P1: 尽快修复
   - P2: 后续优化

4. **具体修复方案**
   - 代码示例
   - 预期收益
   - 风险评估

---

## 🚀 现在开始

### 立即执行：场景 3（无限滚动）

```bash
# 终端 1：观察内存
open -a "Activity Monitor"

# 终端 2：观察日志（过滤关键信息）
tail -f /tmp/agendada-perf.log | grep -E "(attach|loadCard|mergeSchedules)"
```

然后在 Agendada 中：
- 确保有 30+ 个笔记
- 快速上下滚动 2 分钟
- 观察内存和 CPU

完成后告诉我结果！

---

**准备好了吗？从场景 3 开始，每个场景完成后告诉我观察结果。**
