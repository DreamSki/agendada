import AgendadaCore
import SwiftUI

/// Placeholder — the main timeline rendering is now in RelatedPanelContentView
/// which owns the ScrollViewReader for scroll-up/down coordination with the header.
///
/// This file is kept as a convenience namespace if we later extract
/// the permission flow / denied views into a standalone component.
